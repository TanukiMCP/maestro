runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties: {}
  exampleConfig: {}

toolScanning:
  timeout: 30
  retries: 3
  lazyLoad: true
  enabled: true
  mcpVersion: "2025-03-26"